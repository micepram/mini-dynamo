# mini-dynamo
mini-dynamo is a distributed key-value store inspired by Amazon Dynamo. It uses consistent hashing for data partitioning, replication for durability, and quorum-based reads/writes to balance consistency and availability, simulating a decentralized, fault-tolerant system.
